Remove_huggingface.py can be used to remove all huggingface models from your system by removing the huggingface cache.

plagiarism_detection.py loads a huggingface ai plagiarism detection model and performs experiments to tune the ai detection threshold.